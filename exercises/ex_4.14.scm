It won't work because the map expects the underlying implementation language's procedure
as argument other than the procedure object in the implemented language.

So higher order procedure (which expects procedure as argument) can not be primitive.