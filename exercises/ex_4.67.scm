information should at least include :
1. rule name
2. variable bindings, both bounded (to values or other variables) and unbouned

if the rule is already in processing and the 1 and 2 information above is the same
with the current attempt to apply after unification, then stop.